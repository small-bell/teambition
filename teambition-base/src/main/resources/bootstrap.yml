spring:
  cloud:
    config:
      name: base      #config配置文件前面字段
      profile: dev    #config配置文件后面字段
      label: master
      uri: http://localhost:12000

#localhost:12000/actuator/bus-refresh  post提交 热刷新
